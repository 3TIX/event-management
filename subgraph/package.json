{
  "name": "subgrahp",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ hackfs2022/subgraph",
    "create-local": "graph create --node http://localhost:8020/ hackfs2022/subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ hackfs2022/subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 hackfs2022/subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.32.0",
    "@graphprotocol/graph-ts": "0.27.0"
  }
}
